[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ctrl-kd"
version = "0.1.0"
description = "Convert WordStar 4-7 documents and print-to-disk files to text, Markdown, HTML, or RTF. ^KD: save and done."
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Jon Michaels"}]
requires-python = ">=3.9"
keywords = ["wordstar", "converter", "retrocomputing", "archive", "dos"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Text Processing :: Filters",
    "Topic :: System :: Archiving",
]

[project.urls]
Homepage = "https://github.com/jonmichaels/ctrl-kd"

[project.scripts]
ctrl-kd = "ctrlkd.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
